mutation DeleteMediaListEntry($id: Int) {
  DeleteMediaListEntry(id: $id) {
    deleted
  }
}

# This fragment is used to update the cache after the mutation is run.
fragment EntryDelete on Media {
  id
  mediaListEntry
}
